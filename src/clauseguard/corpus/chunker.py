"""Section/clause-aware chunking (~400–512 tokens) (Week 2). Placeholder only."""

from typing import Any


def chunk_document(text: str, *, spec_id: str, source_file: str) -> list[dict[str, Any]]:
    """Chunk parsed spec text. Not implemented in scaffold."""
    raise NotImplementedError("Week 2 — clause-boundary chunking with metadata")
