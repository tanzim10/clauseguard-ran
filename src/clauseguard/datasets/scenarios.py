"""Scenario load helpers (Week 4). Placeholder only."""

from clauseguard.datasets.schema import ScenarioRecord


def load_scenario(scenario_id: str) -> ScenarioRecord:
    """Load one scenario by id. Not implemented in scaffold."""
    raise NotImplementedError("Week 4 — load_scenario(id)")
