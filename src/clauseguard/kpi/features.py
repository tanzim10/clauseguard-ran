"""KPI window feature extraction (Week 6). Placeholder only."""

from typing import Any


def extract_window_stats(kpi_window: Any) -> dict[str, float]:
    """Compute mean/std/slope features. Not implemented in scaffold."""
    raise NotImplementedError("Week 6 — KPI window statistics")
