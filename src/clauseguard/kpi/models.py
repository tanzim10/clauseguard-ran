"""Classical / deep KPI models (Week 6). Placeholder only."""

from typing import Any


def train_classical(features: Any, labels: Any) -> Any:
    """Train classical baseline. Not implemented in scaffold."""
    raise NotImplementedError("Week 6 — classical KPI model")


def train_deep(features: Any, labels: Any) -> Any:
    """Train one deep TS model. Not implemented in scaffold."""
    raise NotImplementedError("Week 6 — deep KPI model")
