"""NL-context-only classifier (Week 6). Placeholder only."""


def classify_from_text(nl_context: str) -> dict[str, str]:
    """Predict fault/rca from text only. Not implemented in scaffold."""
    raise NotImplementedError("Week 6 — text-only classifier")
